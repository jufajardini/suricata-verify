%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - flow
        - stats:
            decoder.invalid: false

app-layer:
  protocols:
    enip:
      enabled: detection-only
      detection-ports:
        dp: 44818
